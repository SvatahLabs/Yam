{"id": "da-001", "window": "Yam", "phrase": "the first recent project", "expect": "present", "element": "project-recent-0", "role": "button", "name": "fixtures", "note": "The Recent list's first entry. Its accessible name is the project's own directory name, so the generated case is \"the fixtures button\" \u2014 and a flow that said that would be a flow about one machine's directory layout."}
{"id": "da-002", "window": "Yam", "phrase": "the Flows rail item", "expect": "present", "element": "rail-flows", "role": "button", "name": "Flows", "note": "\"rail item\" is what LLD \u00a713.7 calls the left rail's rows; the generated phrase is \"the Flows button\"."}
{"id": "da-003", "window": "Yam", "phrase": "the toolbar title", "expect": "present", "element": "toolbar-title", "role": "heading", "name": "Flows", "note": "The heading is named after whichever screen is open, so a phrase built from its name would name the screen rather than the control."}
{"id": "da-004", "window": "Yam", "phrase": "the Runs rail item", "expect": "present", "element": "rail-runs", "role": "button", "name": "Runs", "note": "The left rail's rows are \"rail items\" in LLD \u00a713.7's own words. The generated phrase is \"the X button\", which for Import prototype database names the rail row and the Import screen's action alike \u2014 two controls, one phrase, and a flow that said it would be ambiguous."}
{"id": "da-005", "window": "Yam", "phrase": "the Bindings rail item", "expect": "present", "element": "rail-bindings", "role": "button", "name": "Bindings", "note": "The left rail's rows are \"rail items\" in LLD \u00a713.7's own words. The generated phrase is \"the X button\", which for Import prototype database names the rail row and the Import screen's action alike \u2014 two controls, one phrase, and a flow that said it would be ambiguous."}
{"id": "da-006", "window": "Yam", "phrase": "the Agents and tools rail item", "expect": "present", "element": "rail-agents", "role": "button", "name": "Agents and tools", "note": "The left rail's rows are \"rail items\" in LLD \u00a713.7's own words. The generated phrase is \"the X button\", which for Import prototype database names the rail row and the Import screen's action alike \u2014 two controls, one phrase, and a flow that said it would be ambiguous."}
{"id": "da-007", "window": "Yam", "phrase": "the API rail item", "expect": "present", "element": "rail-api", "role": "button", "name": "API", "note": "The left rail's rows are \"rail items\" in LLD \u00a713.7's own words. The generated phrase is \"the X button\", which for Import prototype database names the rail row and the Import screen's action alike \u2014 two controls, one phrase, and a flow that said it would be ambiguous."}
{"id": "da-008", "window": "Yam", "phrase": "the Data rail item", "expect": "present", "element": "rail-data", "role": "button", "name": "Data", "note": "The left rail's rows are \"rail items\" in LLD \u00a713.7's own words. The generated phrase is \"the X button\", which for Import prototype database names the rail row and the Import screen's action alike \u2014 two controls, one phrase, and a flow that said it would be ambiguous."}
{"id": "da-009", "window": "Yam", "phrase": "the Import prototype database rail item", "expect": "present", "element": "rail-import", "role": "button", "name": "Import prototype database", "note": "The left rail's rows are \"rail items\" in LLD \u00a713.7's own words. The generated phrase is \"the X button\", which for Import prototype database names the rail row and the Import screen's action alike \u2014 two controls, one phrase, and a flow that said it would be ambiguous."}
{"id": "da-010", "window": "Yam", "phrase": "the Settings rail item", "expect": "present", "element": "rail-settings", "role": "button", "name": "Settings", "note": "The left rail's rows are \"rail items\" in LLD \u00a713.7's own words. The generated phrase is \"the X button\", which for Import prototype database names the rail row and the Import screen's action alike \u2014 two controls, one phrase, and a flow that said it would be ambiguous."}
{"id": "da-011", "window": "Yam", "phrase": "the run data table", "expect": "present", "element": "data-table", "role": "table", "name": "RUN DATA", "note": "A container, not a control (T12.7). `scripts/desktop-grounding-cases.mjs` reads the app's *interactive* elements, and pattern 32 asks about the members of a set inside one: \"Every row of the named requests table should be visible\". A container has no generated phrase because it has no role the generator recognises, so its phrase is written here, once, beside the id it will never lose."}
{"id": "da-012", "window": "Yam", "phrase": "the project settings table", "expect": "present", "element": "settings-project", "role": "table", "name": "PROJECT", "note": "A container, not a control (T12.7). `scripts/desktop-grounding-cases.mjs` reads the app's *interactive* elements, and pattern 32 asks about the members of a set inside one: \"Every row of the named requests table should be visible\". A container has no generated phrase because it has no role the generator recognises, so its phrase is written here, once, beside the id it will never lose."}
{"id": "da-013", "window": "Yam", "phrase": "the run settings table", "expect": "present", "element": "settings-run", "role": "table", "name": "RUN", "note": "A container, not a control (T12.7). `scripts/desktop-grounding-cases.mjs` reads the app's *interactive* elements, and pattern 32 asks about the members of a set inside one: \"Every row of the named requests table should be visible\". A container has no generated phrase because it has no role the generator recognises, so its phrase is written here, once, beside the id it will never lose."}
{"id": "da-014", "window": "Yam", "phrase": "the service settings table", "expect": "present", "element": "settings-service", "role": "table", "name": "SERVICE", "note": "A container, not a control (T12.7). `scripts/desktop-grounding-cases.mjs` reads the app's *interactive* elements, and pattern 32 asks about the members of a set inside one: \"Every row of the named requests table should be visible\". A container has no generated phrase because it has no role the generator recognises, so its phrase is written here, once, beside the id it will never lose."}
{"id": "da-015", "window": "Yam", "phrase": "the diagnostics panel", "expect": "present", "element": "inspector-diagnostics", "role": "region", "name": "DIAGNOSTICS", "note": "A container, not a control (T12.7). `scripts/desktop-grounding-cases.mjs` reads the app's *interactive* elements, and pattern 32 asks about the members of a set inside one: \"Every row of the named requests table should be visible\". A container has no generated phrase because it has no role the generator recognises, so its phrase is written here, once, beside the id it will never lose."}
{"id": "da-016", "window": "Yam", "phrase": "the where this came from panel", "expect": "present", "element": "inspector-sources", "role": "region", "name": "WHERE THIS CAME FROM", "note": "A container, not a control (T12.7). `scripts/desktop-grounding-cases.mjs` reads the app's *interactive* elements, and pattern 32 asks about the members of a set inside one: \"Every row of the named requests table should be visible\". A container has no generated phrase because it has no role the generator recognises, so its phrase is written here, once, beside the id it will never lose."}
{"id": "da-017", "window": "Yam", "phrase": "the exposed tools table", "expect": "present", "element": "agents-tools", "role": "table", "name": "TOOLS", "note": "A container, not a control (T12.7). `scripts/desktop-grounding-cases.mjs` reads the app's *interactive* elements, and pattern 32 asks about the members of a set inside one: \"Every row of the named requests table should be visible\". A container has no generated phrase because it has no role the generator recognises, so its phrase is written here, once, beside the id it will never lose."}
{"id": "da-018", "window": "Yam", "phrase": "the invocations table", "expect": "present", "element": "agents-invocations", "role": "table", "name": "INVOCATIONS", "note": "A container, not a control (T12.7). `scripts/desktop-grounding-cases.mjs` reads the app's *interactive* elements, and pattern 32 asks about the members of a set inside one: \"Every row of the named requests table should be visible\". A container has no generated phrase because it has no role the generator recognises, so its phrase is written here, once, beside the id it will never lose."}
{"id": "da-019", "window": "Yam", "phrase": "the refused panel", "expect": "present", "element": "inspector-refused", "role": "region", "name": "REFUSED", "note": "A container, not a control (T12.7). `scripts/desktop-grounding-cases.mjs` reads the app's *interactive* elements, and pattern 32 asks about the members of a set inside one: \"Every row of the named requests table should be visible\". A container has no generated phrase because it has no role the generator recognises, so its phrase is written here, once, beside the id it will never lose."}
{"id": "da-020", "window": "Yam", "phrase": "the named requests table", "expect": "present", "element": "api-requests", "role": "table", "name": "NAMED REQUESTS", "note": "A container, not a control (T12.7). `scripts/desktop-grounding-cases.mjs` reads the app's *interactive* elements, and pattern 32 asks about the members of a set inside one: \"Every row of the named requests table should be visible\". A container has no generated phrase because it has no role the generator recognises, so its phrase is written here, once, beside the id it will never lose."}
{"id": "da-021", "window": "Yam", "phrase": "the request pane", "expect": "present", "element": "api-request", "role": "group", "name": "Request", "note": "A container, not a control (T12.7). `scripts/desktop-grounding-cases.mjs` reads the app's *interactive* elements, and pattern 32 asks about the members of a set inside one: \"Every row of the named requests table should be visible\". A container has no generated phrase because it has no role the generator recognises, so its phrase is written here, once, beside the id it will never lose."}
{"id": "da-022", "window": "Yam", "phrase": "the prototype database field", "expect": "present", "element": "import-source", "role": "textbox", "name": "PROTOTYPE DATABASE", "note": "A container, not a control (T12.7). `scripts/desktop-grounding-cases.mjs` reads the app's *interactive* elements, and pattern 32 asks about the members of a set inside one: \"Every row of the named requests table should be visible\". A container has no generated phrase because it has no role the generator recognises, so its phrase is written here, once, beside the id it will never lose."}
{"id": "da-023", "window": "Yam", "phrase": "the where it may write panel", "expect": "present", "element": "inspector-confinement", "role": "region", "name": "WHERE IT MAY WRITE", "note": "A container, not a control (T12.7). `scripts/desktop-grounding-cases.mjs` reads the app's *interactive* elements, and pattern 32 asks about the members of a set inside one: \"Every row of the named requests table should be visible\". A container has no generated phrase because it has no role the generator recognises, so its phrase is written here, once, beside the id it will never lose."}
{"id": "da-024", "window": "Yam", "phrase": "the terminal panel", "expect": "present", "element": "inspector-cli", "role": "region", "name": "THE SAME THING FROM A TERMINAL", "note": "A container, not a control (T12.7). `scripts/desktop-grounding-cases.mjs` reads the app's *interactive* elements, and pattern 32 asks about the members of a set inside one: \"Every row of the named requests table should be visible\". A container has no generated phrase because it has no role the generator recognises, so its phrase is written here, once, beside the id it will never lose."}
{"id": "da-025", "window": "Yam", "phrase": "the bindings table", "expect": "present", "element": "bindings-table", "role": "table", "name": "BINDINGS", "note": "A container, not a control (T12.7). `scripts/desktop-grounding-cases.mjs` reads the app's *interactive* elements, and pattern 32 asks about the members of a set inside one: \"Every row of the named requests table should be visible\". A container has no generated phrase because it has no role the generator recognises, so its phrase is written here, once, beside the id it will never lose."}
{"id": "da-026", "window": "Yam", "phrase": "the resolver order table", "expect": "present", "element": "inspector-candidate-table", "role": "table", "name": "RESOLVER ORDER", "note": "A container, not a control (T12.7). `scripts/desktop-grounding-cases.mjs` reads the app's *interactive* elements, and pattern 32 asks about the members of a set inside one: \"Every row of the named requests table should be visible\". A container has no generated phrase because it has no role the generator recognises, so its phrase is written here, once, beside the id it will never lose."}
{"id": "da-027", "window": "Yam", "phrase": "the binding panel", "expect": "present", "element": "inspector-binding", "role": "region", "name": "APP.BOOKINGS-LINK", "note": "A container, not a control (T12.7). `scripts/desktop-grounding-cases.mjs` reads the app's *interactive* elements, and pattern 32 asks about the members of a set inside one: \"Every row of the named requests table should be visible\". A container has no generated phrase because it has no role the generator recognises, so its phrase is written here, once, beside the id it will never lose."}
{"id": "da-028", "window": "Yam", "phrase": "the fingerprint panel", "expect": "present", "element": "inspector-fingerprint", "role": "region", "name": "FINGERPRINT", "note": "A container, not a control (T12.7). `scripts/desktop-grounding-cases.mjs` reads the app's *interactive* elements, and pattern 32 asks about the members of a set inside one: \"Every row of the named requests table should be visible\". A container has no generated phrase because it has no role the generator recognises, so its phrase is written here, once, beside the id it will never lose."}
{"id": "da-029", "window": "Yam", "phrase": "the runs worth healing table", "expect": "present", "element": "heal-candidates", "role": "table", "name": "RUNS WITH A FAILURE", "note": "A container, not a control (T12.7). `scripts/desktop-grounding-cases.mjs` reads the app's *interactive* elements, and pattern 32 asks about the members of a set inside one: \"Every row of the named requests table should be visible\". A container has no generated phrase because it has no role the generator recognises, so its phrase is written here, once, beside the id it will never lose."}
{"id": "da-030", "window": "Yam", "phrase": "the proposals table", "expect": "present", "element": "heal-proposals", "role": "table", "name": "PROPOSALS", "note": "A container, not a control (T12.7). `scripts/desktop-grounding-cases.mjs` reads the app's *interactive* elements, and pattern 32 asks about the members of a set inside one: \"Every row of the named requests table should be visible\". A container has no generated phrase because it has no role the generator recognises, so its phrase is written here, once, beside the id it will never lose."}
{"id": "da-031", "window": "Yam", "phrase": "the intent field", "expect": "present", "element": "explorer-intent", "role": "textbox", "name": "INTENT", "note": "A container, not a control (T12.7). `scripts/desktop-grounding-cases.mjs` reads the app's *interactive* elements, and pattern 32 asks about the members of a set inside one: \"Every row of the named requests table should be visible\". A container has no generated phrase because it has no role the generator recognises, so its phrase is written here, once, beside the id it will never lose."}
{"id": "da-032", "window": "Yam", "phrase": "the intent required alert", "expect": "present", "element": "explorer-intent-required", "role": "alert", "name": "", "note": "A container, not a control (T12.7). `scripts/desktop-grounding-cases.mjs` reads the app's *interactive* elements, and pattern 32 asks about the members of a set inside one: \"Every row of the named requests table should be visible\". A container has no generated phrase because it has no role the generator recognises, so its phrase is written here, once, beside the id it will never lose."}
{"id": "da-033", "window": "Yam", "phrase": "the trajectory table", "expect": "present", "element": "explorer-calls", "role": "table", "name": "TRAJECTORY", "note": "A container, not a control (T12.7). `scripts/desktop-grounding-cases.mjs` reads the app's *interactive* elements, and pattern 32 asks about the members of a set inside one: \"Every row of the named requests table should be visible\". A container has no generated phrase because it has no role the generator recognises, so its phrase is written here, once, beside the id it will never lose."}
{"id": "da-034", "window": "Yam", "phrase": "the session panel", "expect": "present", "element": "inspector-session", "role": "region", "name": "SESSION", "note": "A container, not a control (T12.7). `scripts/desktop-grounding-cases.mjs` reads the app's *interactive* elements, and pattern 32 asks about the members of a set inside one: \"Every row of the named requests table should be visible\". A container has no generated phrase because it has no role the generator recognises, so its phrase is written here, once, beside the id it will never lose."}
{"id": "da-035", "window": "Yam", "phrase": "the workspace", "expect": "present", "element": "workspace", "role": "region", "name": "Workspace", "note": "A container, not a control (T12.7). `scripts/desktop-grounding-cases.mjs` reads the app's *interactive* elements, and pattern 32 asks about the members of a set inside one: \"Every row of the named requests table should be visible\". A container has no generated phrase because it has no role the generator recognises, so its phrase is written here, once, beside the id it will never lose."}
{"id": "da-036", "window": "Yam", "phrase": "the section rail", "expect": "present", "element": "rail", "role": "navigation", "name": "Sections", "note": "A container, not a control (T12.7). `scripts/desktop-grounding-cases.mjs` reads the app's *interactive* elements, and pattern 32 asks about the members of a set inside one: \"Every row of the named requests table should be visible\". A container has no generated phrase because it has no role the generator recognises, so its phrase is written here, once, beside the id it will never lose."}
{"id": "da-037", "window": "Yam", "phrase": "the run steps pane", "expect": "present", "element": "run-steps", "role": "group", "name": "Steps", "note": "A container, not a control (T12.7). `scripts/desktop-grounding-cases.mjs` reads the app's *interactive* elements, and pattern 32 asks about the members of a set inside one: \"Every row of the named requests table should be visible\". A container has no generated phrase because it has no role the generator recognises, so its phrase is written here, once, beside the id it will never lose."}
{"id": "da-038", "window": "Yam", "phrase": "the run audit list", "expect": "present", "element": "run-audit", "role": "list", "name": "Audit", "note": "A container, not a control (T12.7). `scripts/desktop-grounding-cases.mjs` reads the app's *interactive* elements, and pattern 32 asks about the members of a set inside one: \"Every row of the named requests table should be visible\". A container has no generated phrase because it has no role the generator recognises, so its phrase is written here, once, beside the id it will never lose."}
{"id": "da-039", "window": "Yam", "phrase": "the stories table", "expect": "present", "element": "run-stories", "role": "table", "name": "STORIES IN THIS RUN", "note": "A container, not a control (T12.7). `scripts/desktop-grounding-cases.mjs` reads the app's *interactive* elements, and pattern 32 asks about the members of a set inside one: \"Every row of the named requests table should be visible\". A container has no generated phrase because it has no role the generator recognises, so its phrase is written here, once, beside the id it will never lose."}
{"id": "da-040", "window": "Yam", "phrase": "the flows list", "expect": "present", "element": "flows-list", "role": "table", "name": "FLOW FILES", "note": "A container, not a control (T12.7). `scripts/desktop-grounding-cases.mjs` reads the app's *interactive* elements, and pattern 32 asks about the members of a set inside one: \"Every row of the named requests table should be visible\". A container has no generated phrase because it has no role the generator recognises, so its phrase is written here, once, beside the id it will never lose."}
{"id": "da-041", "window": "Yam", "phrase": "the command palette", "expect": "present", "element": "command-palette", "role": "dialog", "name": "Command palette", "note": "A container, not a control (T12.7). `scripts/desktop-grounding-cases.mjs` reads the app's *interactive* elements, and pattern 32 asks about the members of a set inside one: \"Every row of the named requests table should be visible\". A container has no generated phrase because it has no role the generator recognises, so its phrase is written here, once, beside the id it will never lose."}
{"id": "da-042", "window": "Yam", "phrase": "the command list", "expect": "present", "element": "command-palette-rows", "role": "listbox", "name": "Commands", "note": "A container, not a control (T12.7). `scripts/desktop-grounding-cases.mjs` reads the app's *interactive* elements, and pattern 32 asks about the members of a set inside one: \"Every row of the named requests table should be visible\". A container has no generated phrase because it has no role the generator recognises, so its phrase is written here, once, beside the id it will never lose."}
{"id": "da-043", "window": "Yam", "phrase": "the go to Flows row", "expect": "present", "element": "palette-go-flows", "role": "option", "name": "Go to Flows", "note": "A container, not a control (T12.7). `scripts/desktop-grounding-cases.mjs` reads the app's *interactive* elements, and pattern 32 asks about the members of a set inside one: \"Every row of the named requests table should be visible\". A container has no generated phrase because it has no role the generator recognises, so its phrase is written here, once, beside the id it will never lose."}
{"id": "da-044", "window": "Yam", "phrase": "the go to Record row", "expect": "present", "element": "palette-go-record", "role": "option", "name": "Go to Record", "note": "A container, not a control (T12.7). `scripts/desktop-grounding-cases.mjs` reads the app's *interactive* elements, and pattern 32 asks about the members of a set inside one: \"Every row of the named requests table should be visible\". A container has no generated phrase because it has no role the generator recognises, so its phrase is written here, once, beside the id it will never lose."}
{"id": "da-045", "window": "Yam", "phrase": "the go to Run row", "expect": "present", "element": "palette-go-run", "role": "option", "name": "Go to Run", "note": "A container, not a control (T12.7). `scripts/desktop-grounding-cases.mjs` reads the app's *interactive* elements, and pattern 32 asks about the members of a set inside one: \"Every row of the named requests table should be visible\". A container has no generated phrase because it has no role the generator recognises, so its phrase is written here, once, beside the id it will never lose."}
{"id": "da-046", "window": "Yam", "phrase": "the go to Heal row", "expect": "present", "element": "palette-go-heal", "role": "option", "name": "Go to Heal", "note": "A container, not a control (T12.7). `scripts/desktop-grounding-cases.mjs` reads the app's *interactive* elements, and pattern 32 asks about the members of a set inside one: \"Every row of the named requests table should be visible\". A container has no generated phrase because it has no role the generator recognises, so its phrase is written here, once, beside the id it will never lose."}
{"id": "da-047", "window": "Yam", "phrase": "the go to Explorer row", "expect": "present", "element": "palette-go-explorer", "role": "option", "name": "Go to Explorer", "note": "A container, not a control (T12.7). `scripts/desktop-grounding-cases.mjs` reads the app's *interactive* elements, and pattern 32 asks about the members of a set inside one: \"Every row of the named requests table should be visible\". A container has no generated phrase because it has no role the generator recognises, so its phrase is written here, once, beside the id it will never lose."}
{"id": "da-048", "window": "Yam", "phrase": "the inspector panel", "expect": "present", "element": "inspector", "role": "complementary", "name": "Inspector", "note": "A container, not a control (T12.7). `scripts/desktop-grounding-cases.mjs` reads the app's *interactive* elements, and pattern 32 asks about the members of a set inside one: \"Every row of the named requests table should be visible\". A container has no generated phrase because it has no role the generator recognises, so its phrase is written here, once, beside the id it will never lose."}
{"id": "da-049", "window": "Yam", "phrase": "the fake gateway note", "expect": "present", "element": "record-fake-gateway", "role": "group", "name": "", "note": "A container, not a control (T12.7). `scripts/desktop-grounding-cases.mjs` reads the app's *interactive* elements, and pattern 32 asks about the members of a set inside one: \"Every row of the named requests table should be visible\". A container has no generated phrase because it has no role the generator recognises, so its phrase is written here, once, beside the id it will never lose."}
{"id": "da-050", "window": "Yam", "phrase": "the flows to record table", "expect": "present", "element": "record-flows", "role": "table", "name": "FLOWS TO RECORD", "note": "A container, not a control (T12.7). `scripts/desktop-grounding-cases.mjs` reads the app's *interactive* elements, and pattern 32 asks about the members of a set inside one: \"Every row of the named requests table should be visible\". A container has no generated phrase because it has no role the generator recognises, so its phrase is written here, once, beside the id it will never lose."}
{"id": "da-051", "window": "Yam", "phrase": "the runs table", "expect": "present", "element": "runs-table", "role": "table", "name": "RUNS", "note": "A container, not a control (T12.7). `scripts/desktop-grounding-cases.mjs` reads the app's *interactive* elements, and pattern 32 asks about the members of a set inside one: \"Every row of the named requests table should be visible\". A container has no generated phrase because it has no role the generator recognises, so its phrase is written here, once, beside the id it will never lose."}
{"id": "da-052", "window": "Yam", "phrase": "the run inspector panel", "expect": "present", "element": "inspector-run", "role": "region", "name": "", "note": "A container, not a control (T12.7). `scripts/desktop-grounding-cases.mjs` reads the app's *interactive* elements, and pattern 32 asks about the members of a set inside one: \"Every row of the named requests table should be visible\". A container has no generated phrase because it has no role the generator recognises, so its phrase is written here, once, beside the id it will never lose."}
{"id": "da-053", "window": "Yam", "phrase": "the artifacts panel", "expect": "present", "element": "inspector-artifacts", "role": "region", "name": "WHAT IT WROTE", "note": "A container, not a control (T12.7). `scripts/desktop-grounding-cases.mjs` reads the app's *interactive* elements, and pattern 32 asks about the members of a set inside one: \"Every row of the named requests table should be visible\". A container has no generated phrase because it has no role the generator recognises, so its phrase is written here, once, beside the id it will never lose."}
