# The same control, the same id, the other adapter (T11.2, LLD §13.9).
#
# The AX side binds `automationId`, which on macOS Chromium publishes as
# `AXDOMIdentifier` — the DOM `id`. So the web side binds a `css` `#id`, and it
# is the *same fact about the application* said in the vocabulary each adapter
# has. That is what makes a parity gate a comparison rather than two suites.
schemaVersion: "1.0.0"
id: "app.first-recent-project"
phrases:
  - "the first recent project"
entries:
  - candidates:
      - by: "css"
        value: "#project-recent-0"
        score: 0.99
    context:
      platform: "web"
      pattern: "/"
      hash: "25da5590b52a8019f790a9d2bda9910517f510cb7f7bc8feb5afbb5ae90055ec"
    fingerprint:
      tag: "button"
      attrs:
        id: "project-recent-0"
      text: ""
      neighbours:
        before: []
        after: []
      rolePath: []
      box: [0, 0, 0, 0]
      index: 0
    provenance:
      at: "2026-09-05T00:00:00.000Z"
      model: "none"
      promptVersion: "seeded"
      tokensIn: 0
      tokensOut: 0
      costUsd: 0
    recordedAt: "2026-09-05T00:00:00.000Z"
    verified: false
