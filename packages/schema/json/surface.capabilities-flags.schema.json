{
  "$id": "https://yam.svatah.com/schema/1.0.0/surface.capabilities-flags.schema.json",
  "$ref": "#/definitions/surface.capabilities-flags",
  "$schema": "http://json-schema.org/draft-07/schema#",
  "definitions": {
    "surface.capabilities-flags": {
      "additionalProperties": false,
      "properties": {
        "dialogs": {
          "type": "boolean"
        },
        "drag": {
          "type": "boolean"
        },
        "frames": {
          "type": "boolean"
        },
        "observe": {
          "type": "boolean"
        },
        "pick": {
          "type": "boolean"
        },
        "restore": {
          "type": "boolean"
        },
        "screenshot": {
          "type": "boolean"
        },
        "trace": {
          "type": "boolean"
        },
        "upload": {
          "type": "boolean"
        },
        "webmcp": {
          "type": "boolean"
        },
        "windowChrome": {
          "type": "boolean"
        },
        "windows": {
          "type": "boolean"
        }
      },
      "required": [
        "dialogs",
        "frames",
        "windows",
        "upload",
        "drag",
        "trace",
        "webmcp",
        "screenshot",
        "restore",
        "pick",
        "observe",
        "windowChrome"
      ],
      "type": "object"
    }
  },
  "description": "Agent surface: the capability flags (LLD §2.4). Yam schemaVersion 1.0.0.",
  "title": "surface.capabilities-flags",
  "x-yam-schema-version": "1.0.0"
}
