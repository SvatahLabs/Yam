# Node 22 LTS is the only supported runtime (REQ-NFR-7). engine-strict is left off
# so the repo still installs on newer Node for local experimentation; CI pins 22.
engine-strict=false
provenance=false
